# Copy to .env. Optional — the client falls back to http://localhost:5000/api in
# dev and to a same-origin /api in production builds, so Vercel needs no value.
VITE_API_URL=http://localhost:5000/api
